# Install only from competition-approved resources in the competition container.
# Keep the platform's Blackwell-compatible torch/CUDA build; do not downgrade to old baseline pins.
torch>=2.7
torchvision
timm
numpy
nibabel
pydicom>=3
openpyxl
scikit-learn
pandas
PyYAML
fastapi
pydantic>=2
uvicorn
requests
# Cloud engineering tests (FastAPI TestClient):
httpx
# Compressed DICOM may additionally need platform-provided pylibjpeg codecs.
