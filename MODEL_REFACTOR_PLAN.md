# 真假人体、拼接与重复影像识别：改造及基线模型接入方案

本文说明当前代码能复用什么、需要修改哪些入口，以及选择赛事基线后应把模型接在哪里。现有项目的运行命令和输出说明见 [README.md](README.md)。

**文档状态：保留的历史设计方案。** 下文的“现有／建议新增／待确认”均对应撰写时的旧代码状态，不代表当前实现。2026-09-14 已根据确认方案新增目标一、二实现，实际入口、基线选择、数据前提及容器验收方式见 [DETECTION_README.md](DETECTION_README.md)。尚未进行真实赛事数据训练或容器运行验收。

文中的状态约定：

- **现有**：当前源码已经实现的行为或文件。
- **建议新增 / 建议修改**：后续开发工作，目前尚未执行。
- **待确认**：需要用户、实际基线代码或赛事说明提供依据，本文不假定答案。

## 1. 已明确的需求与待确认边界

### 1.1 已明确

目标系统需要提供三项能力：

1. 真假人体影像识别。
2. 拼接影像识别。
3. 重复影像识别。

用户已决定保留 `true / fake / compositing` 三分类，并计划从赛事提供的基线中选择适用模型进行接入。

### 1.2 本稿的架构建议及其前提

**建议把“一个模型”落实为一个统一预测系统，内部有两个任务模型：三分类模型 + 重复检测模型。** 两个模型独立训练、独立保存权重，共用命令行和服务入口。三项能力不等于必须训练三个模型。

架构形式已向用户询问；在尚未得到明确答复时，以上属于建议方案，不代表用户已经确认“两套网络”。本文后续文件规划按此方案展开。如果要求一个共享骨干、联合训练的单一网络，需要额外设计联合数据加载、分类/配对损失、任务采样、预处理兼容性和统一 checkpoint；不能通过简单合并两份权重实现。

**尚未收到具体赛事基线的名称、代码或接口资料。** 因此本文不会指定某个模型、编造构造函数、预训练权重文件名或最佳超参数。文中的模型构造函数名和新增文件名均是本项目的拟定接口，不是赛事基线已有 API。

重复检测暂按当前实现的“病例级比较”描述。如果需要定位到具体重复序列或切片，应在选型前确认；这会改变数据、模型和结果的粒度。

### 1.3 三分类的含义与输出边界

现有标签映射位于 [src/tasks/abnormal/dataset.py](src/tasks/abnormal/dataset.py)：

```python
LABEL_MAP = {"true": 0, "fake": 1, "compositing": 2}
```

按当前代码用途，`true` 是真实来源影像类，`fake` 对应非人体影像类，`compositing` 对应拼接类。`true` 不表示没有病灶或肿瘤。最终标注边界以赛事说明和实际数据为准。

现有推理对三分类概率取：

- 第 1 项：`p_true`，当前没有单独输出到病例 JSON。
- 第 2 项：`p_fake`，写入 `IsNotHumanBodyProb`。
- 第 3 项：`p_compositing`，写入 `IsStitchedProb`。

不能把 `p_true` 直接命名为“人体概率”：拼接类别也可能由人体影像组成。若需要“人体 / 非人体”的额外二值结果，应先确认官方定义，再规定概率转换和阈值。三分类内部三项概率和为 1，但重复分值是另一任务的输出，不参与这个和。

## 2. 当前项目结构与实际依赖

### 2.1 目录结构

```text
项目根目录/
├── configs/config.yaml                  # 路径、训练及推理配置
├── train/
│   ├── train_abnormal.py                # true/fake/compositing 三分类
│   ├── train_duplicate.py               # 病例重复检测
│   ├── train_sequence.py                # 序列类别识别
│   ├── train_segmentation.py            # core/abnormal 两个分割模型
│   └── train_characteristics.py         # 病例影像特征预测
├── scripts/
│   ├── validate_data.py                 # 数据检查
│   ├── test_pipeline.py                 # 完整批量推理
│   └── simulate_validation.py           # 真实类别病例的模拟推理
├── service/server.py                    # HTTP 接口和后台任务
└── src/
    ├── common/                          # 配置、日志、设备、checkpoint
    ├── data/                            # NIfTI、路径规则、病例划分
    ├── models/basic3d.py                # 多个任务复用的网络类定义
    ├── tasks/
    │   ├── abnormal/dataset.py          # 三分类数据集；此目录还没有 model.py
    │   ├── duplicate/dataset.py         # 重复正负样本对
    │   ├── duplicate/model.py           # 病例编码与相似度
    │   └── sequence、segmentation、characteristics/
    └── pipeline/
        ├── load_models.py              # 无条件加载全部 6 个模型
        ├── case_pipeline.py            # 单病例完整推理和结果 JSON
        ├── duplicate_pipeline.py       # 批次内重复候选对
        └── batch_pipeline.py           # 串联单病例流程与重复检测
```

### 2.2 两个目标模型当前在哪里

**三分类：** [train/train_abnormal.py](train/train_abnormal.py) 直接创建 `VolumeClassifier(num_classes=3)`，类定义在 [src/models/basic3d.py](src/models/basic3d.py)。使用 `CrossEntropyLoss`，按验证集三分类整体准确率保存最佳权重。

**重复检测：** [train/train_duplicate.py](train/train_duplicate.py) 创建 [src/tasks/duplicate/model.py](src/tasks/duplicate/model.py) 中的 `DuplicateModel`。它对每个序列编码，对同病例序列向量取平均并归一化，再计算两个病例的余弦相似度。训练使用自定义 `pair_loss`，按验证损失保存最佳权重。

两者不读取序列、分割、特征预测模型的权重，也不读取对方的模型输出。重复训练从 `1_abnormal.xlsx` 获取真实病例作为负样本来源，这属于标签依赖，不是模型依赖。

### 2.3 为什么已有入口不能只跑这三项能力

当前调用关系为：

```text
scripts/test_pipeline.py 或 service/server.py
  → load_all_models()：加载异常、序列、两个分割、特征、重复，共 6 份权重
  → run_batch()
      → 每个病例 run_case()
          → 三分类
          → 序列识别
          → 分割
          → 特征预测
          → 保存完整 prediction.json
      → run_duplicate_pipeline()
```

[src/pipeline/load_models.py](src/pipeline/load_models.py) 没有按任务选择加载的逻辑；[src/pipeline/case_pipeline.py](src/pipeline/case_pipeline.py) 在输出 JSON 前还会使用序列、分割和特征模型。只删除另外四份权重或设置分类数配置，无法改变这条执行链。

重复检测的算法本身独立，但当前批处理把它放在所有病例主流程之后；前面的异常可能阻止重复检测执行。

### 2.4 哪些共享内容不宜直接替换

`Small3DEncoder` 同时被异常、序列、特征和重复模型使用；`VolumeClassifier` 同时用于异常与序列分类。它们共享的是类定义，训练参数并未共享。

因此，直接把 `basic3d.py` 的公共类改成新基线，会改变其他任务的模型结构，可能造成旧权重不兼容。建议把选中基线封装在各自任务目录，只替换目标任务的构造调用。

`src/data/nifti.py` 的预处理也被多任务复用。若两个基线要求不同预处理，不应通过全局改动强迫两者使用同一套输入。

## 3. 建议的目标结构

先保持现有任务边界，增加一条只处理三项能力的公共推理流程：

```text
影像输入
  ├── 三分类任务自己的预处理
  │     → 三分类模型
  │     → fake 概率、compositing 概率及类别信息
  └── 重复任务自己的预处理
        → 病例编码或成对比较
        → 重复候选对及分值

两类结果由同一个命令行入口 / HTTP 服务统一写出
```

两个分支在逻辑上独立；这不要求并行占用 GPU。初版可以顺序执行，也不默认用三分类结果过滤重复检测输入。否则三分类误判会直接影响重复候选覆盖率，而当前代码没有验证这种过滤策略。

建议新增一个公共文件 `src/pipeline/abnormal_duplicate_pipeline.py`，集中放置：

- 只加载三分类和重复模型的函数。
- 三分类单病例预测及病例内聚合。
- 调用重复检测并写结果的批量函数。

再新增轻量命令行入口 `scripts/test_abnormal_duplicate.py`，只负责参数解析、配置、设备选择和调用公共流程。需要服务时，`service/server.py` 调用同一个公共流程，避免命令行和服务分别维护一套预测逻辑。

这些文件目前均不存在。先使用项目现有两个模型验证独立流程，再逐个替换基线，便于判断问题来自流程还是模型适配。

## 4. 文件改造清单

以下“必需”指采用第 3 节方案并接入新基线时的工作，不表示这些修改已经完成。

### 4.1 新增 `src/tasks/abnormal/model.py`：三分类基线接入口

**必需新增。** 当前异常任务没有自己的模型文件。

拟定一个明确的 `build_abnormal_model(...)` 构造函数，供训练和新推理流程共同调用。函数内部负责创建选中的基线结构及三分类输出头；训练初始化时按实际格式读取基线预训练权重。

第一步可让它返回现有 `VolumeClassifier(3)`，先验证调用链；选定基线后再替换内部实现。只有一两个确定模型时，普通函数或简单分支即可，不需要提前建设动态插件注册系统。

### 4.2 修改 `train/train_abnormal.py`：训练接入与评价

**接入新三分类基线时必改。**

- 将直接创建 `VolumeClassifier(3)` 改为调用任务自己的构造函数。
- 明确何时加载基线预训练参数、哪些参数参与训练，以及分类头如何初始化。
- 保留 `true=0 / fake=1 / compositing=2` 的标签约定，保证推理含义一致。
- 如果模型仍返回三分类 logits，可继续使用现有交叉熵；其他输出类型需要显式适配。
- 在整体准确率之外，记录各类别的 precision、recall、F1 和混淆矩阵；最佳权重的选择指标需要明确，不凭空指定赛事评分公式。
- 保存实际病例划分和实验元数据，便于与其他基线进行同划分比较。

保留现有权重位置规则：`<checkpoints_dir>/abnormal/epoch_<轮数>.pth` 和 `best.pth`；日志名为 `abnormal_train.jsonl`。新实验使用独立权重根目录，避免覆盖旧结构的权重。

### 4.3 修改 `src/tasks/duplicate/model.py`：重复检测基线接入口

**接入新重复基线时必改。**

这里承接基线编码器、病例聚合或成对比较模型，并增加供训练和推理共用的构造函数，例如拟定的 `build_duplicate_model(...)`。

如果基线提供可用的独立影像特征向量，可以适配现有 `encode_case()` / `similarity()`；如果只能同时输入两例进行判重，则需要连同训练损失和 `duplicate_pipeline.py` 的评分流程修改，不能假定它支持独立编码。

### 4.4 修改 `train/train_duplicate.py`：基线训练与样本划分

**接入新重复基线时必改。**

- 通过任务构造函数创建与推理一致的网络。
- 核对当前 `pair_loss` 是否适用于基线输出。余弦分值、距离、二分类 logits 和概率不能混用同一个损失。
- 调整参数优化方式、训练批量或采样时，遵循实际模型输入，不能只修改配置中的 `training.batch_size`；当前该入口固定 batch size 为 1，循环只处理 `xa_list[0]`、`xb_list[0]`。提高批量还需要同步处理其余样本对。
- 保存训练/验证病例和实际正负样本对，明确验证样本数量。
- 验证集为空、只有一类或指标未定义时，应标记为不可评价，不能把当前默认的 0 当作可靠分数。

保留权重位置规则：`<checkpoints_dir>/duplicate/epoch_<轮数>.pth` 和 `best.pth`；日志名为 `duplicate_train.jsonl`。

### 4.5 新增 `src/pipeline/abnormal_duplicate_pipeline.py`：公共预测流程

**只运行目标三项能力时必需新增。**

- 使用两个任务模型的同一构造函数，加载各自训练完成的 checkpoint，再进入推理模式。
- 提取 `case_pipeline.py` 中三分类预测的逻辑，输出 `IsNotHumanBodyProb`、`IsStitchedProb`；不调用序列、分割和特征模型。
- 与训练/验证共用约定好的预处理和病例聚合方式。
- 复用或适配 `run_duplicate_pipeline()`，在结果根目录生成重复候选对。
- 创建结果目录，并明确无效影像、空病例和部分失败的处理方式。缺失结果不能伪装成概率 0 或正常完成。

本方案不调用旧的 `load_all_models()`。也不要为了复用 `_load_state()` 而把旧加载模块整体导入：该模块顶层还导入了分割模型，会带入 MONAI 等非目标模型依赖。新的加载逻辑应只导入实际需要的任务模块。

### 4.6 新增 `scripts/test_abnormal_duplicate.py`：统一命令行入口

**本地独立推理时必需新增。**

建议沿用已有 `--config`、`--input`、`--output` 参数形式，调用第 4.5 节公共流程。它是预测入口，不自动等同于评价脚本。

不要在这个脚本里再次实现模型结构或另一套预处理，否则以后换基线还要同步修改多个位置。

### 4.7 配置：建议新增 `configs/config_detection.yaml`

从现有 `config.yaml` 复制并调整，作为三项能力的独立实验配置；保持原配置可追溯。

配置需要明确记录：两个任务的基线身份、预训练参数位置、最终 checkpoint 位置、输入规格、实验种子及输出目录。具体模型字段应在基线选定后确定。

目前 `abnormal.num_classes` 虽然存在，但训练和加载器都硬编码为 3；仅修改 YAML 不会替换模型。未来新增的 `pretrained_path` 等字段也必须有代码读取，不能把配置示例当成已有功能。

`src/common/config.py` 当前只自动解析 `labels_dir`、`checkpoints_dir`、`output_dir` 的相对路径。新增预训练路径若支持相对路径，要在那里补充明确解析规则，或先使用实际绝对路径；不能依赖当前未实现的自动解析。

### 4.8 数据集与预处理：按基线要求修改

- [src/tasks/abnormal/dataset.py](src/tasks/abnormal/dataset.py)：三分类数据、来源路径、采样和训练增强；基线需要不同输入时修改。
- [src/tasks/duplicate/dataset.py](src/tasks/duplicate/dataset.py)：正负配对、病例读取和重复任务预处理；更换比较粒度或采样策略时修改。
- [src/data/nifti.py](src/data/nifti.py)：优先复用原始 NIfTI 读取。若需要新增通用操作，可在这里提供；任务特有变换放在对应任务内，由数据集和推理共同调用。
- [src/data/paths.py](src/data/paths.py)：只有实际数据组织不同才需改。当前 `Label` 还参与来源路径解析，不能随意重命名或丢失。
- [src/data/split.py](src/data/split.py)：可复用保存/读取划分的功能；若有患者或影像来源分组约束，需要增加相应划分逻辑，而不是继续只按目录编号随机划分。

如果某任务的预处理确实需要独立代码，可新增该任务的 `preprocess.py`。这是条件项，不要求现在就创建空文件。

### 4.9 `src/pipeline/duplicate_pipeline.py`：何时需要修改

若新基线完全保留“独立编码病例 + 余弦相似度”的接口，可以先复用主逻辑。以下情况需要修改：

- 基线要求不同预处理、病例聚合或特征归一化。
- 基线采用成对比较，无法生成可复用的独立病例向量。
- 结果要从病例级细化为序列级或切片级。
- 要使用经过验证的阈值、校准方法或不同的候选检索方式。

现有文件从 `case_pipeline.py` 导入 `list_series()`，这是目录工具函数依赖，不会执行其他模型。为减少模块引用，可把该函数移到 `src/data/paths.py`，同步修改两个调用处；这不是首轮独立推理的必要条件。

### 4.10 `service/server.py`：需要 HTTP 时修改

**目标系统要提供 HTTP 服务时必改，本地研究阶段可以后置。**

- `main()`：从加载全部 6 个模型改为加载两个目标模型。
- `background_inference()`：改为调用新的公共批处理流程。
- 复用现有请求接收、结果目录和回调逻辑，并核对官方协议是否仍适用。
- 明确必要字段校验、部分失败状态及相同评测编号的输出处理；不能仅以 `received` 判断推理成功。

`track_code` 当前只被接收，没有用于选择任务。不能假设平台发送某个值就会自动让现有服务只执行三项能力。

### 4.11 检查与评价脚本

**建议修改 `scripts/validate_data.py`：**增加明确的任务范围选项，让目标模式只要求 `1_abnormal.xlsx`、`2_duplicate.xlsx`，并补充重复正样本两侧影像的逐项检查。当前它只检查 `2_duplicate.xlsx` 是否存在，反而会报告其他三个标签表缺失。

**不直接复用 `scripts/simulate_validation.py` 作为目标评估：**它只选 `true` 病例，缺少 fake/compositing 正样本，也不会计算指标。建议新增一个明确读取固定划分和标签的评价入口，例如 `scripts/evaluate_abnormal_duplicate.py`，或先在两个训练入口完善验证逻辑；不要把模拟预测结果当作完整评价。

独立评价入口及参数尚未实现，本文不提供冒充现有功能的评价命令。

### 4.12 哪些旧文件可以保留

按推荐的独立流程方案，以下文件不需要为了目标三项能力而修改：

- `train/train_sequence.py`、`train/train_segmentation.py`、`train/train_characteristics.py`。
- 对应的序列、分割、特征任务代码。
- `scripts/test_pipeline.py`、`src/pipeline/batch_pipeline.py` 和旧 `case_pipeline.py` 的完整流程。
- `src/models/basic3d.py` 的公共网络类。

**旧流程的兼容边界：**如果把新结构的权重放到旧 `load_models.py` 所读的位置，旧加载器仍会构造原网络，可能加载失败。保留旧代码时，应给新实验独立的 `checkpoints_dir`；若未来要求旧完整流程也使用新模型，再同步修改旧加载器里的 abnormal/duplicate 构造调用。

## 5. 基线模型具体接在哪里

### 5.1 接入三分类基线

基线结构的本项目接入点：**建议新增的 `src/tasks/abnormal/model.py`**。

训练路径与部署路径应共享结构定义：

```text
train/train_abnormal.py
  → build_abnormal_model(...)              # 拟定接口
      → 基线网络 + 三分类输出
      → 训练初始化时读取基线预训练参数
  → 训练
  → 保存本项目 abnormal/best.pth

新的公共推理流程
  → 同一个 build_abnormal_model(...)
  → 读取本项目 abnormal/best.pth
  → 推理
```

**现有兼容接口：**输入为浮点张量 `[B, 1, D, H, W]`；输出为未做 softmax 的 logits `[B, 3]`，类别顺序固定为 `[true, fake, compositing]`。单个样本来自一个 3D NIfTI 序列。

这只是现有项目接口，不代表待选基线天然满足它：

- 若基线已有相同三分类任务，先核对类别顺序及预处理，再决定直接微调还是替换头。
- 若基线只有人体真假二分类，它没有自动具备拼接类别；需要可训练结构、适当的三分类输出及拼接标签训练。不能凭二分类分数推导第三类概率。
- 若基线只有编码器，可以接三分类头，但必须核实编码器输出维度和训练方式。
- 若基线是 2D 切片模型，需要定义取片、方向、通道、切片聚合；不能仅把 3D 张量改个尺寸就视为接入完成。
- 若基线返回概率或字典，需要显式适配输出，并保证交叉熵接收正确的 logits；不要重复执行 softmax。

当前病例级分值是对所有有效序列的概率平均。可以先保留为对照方案；若基线采用不同病例聚合方法，必须在验证和正式推理中使用同一方法，并用实际验证结果决定，本文不预设最佳策略。

### 5.2 接入重复检测基线

基线结构的本项目接入点：**现有 `src/tasks/duplicate/model.py`**。

现有接口形状为：

```text
单病例多个序列：x 为 [S, 1, D, H, W]
encode_case(x) → [1, E]
similarity(xa, xb) → [1]
```

其中 `S` 是一个病例的序列数，两个病例可以不同；`E` 是特征维度，当前默认 128。训练批量固定为一对病例，不是固定两个病例必须有相同序列数。

按基线能力分两种接法：

**基线支持独立编码：**在 `DuplicateModel` 内替换编码器，按基线要求处理病例聚合和归一化，保持上述接口时可复用较多训练与候选生成代码。若相似度算法改变，即使张量形状不变，也要同步核对训练和推理的评分规则。

**基线只能成对判别：**适配 `similarity()` 或提供明确的成对评分接口，同时修改训练损失与 `duplicate_pipeline.py`。当前 `E @ E.T` 只适用于独立向量相似度，不能直接替代成对模型。候选生成与成对重排是否需要分开，取决于实际数据规模和基线能力，不在缺少信息时预先指定检索库。

当前 `duplicate_pipeline.py` 并不调用 `model.similarity()`，而是自行归一化向量后计算余弦相似度。因此，**只修改训练时的 `similarity()`，并不会自动改变部署分值**。

### 5.3 预训练权重与训练后权重是两回事

基线可能提供裸 `state_dict`、带嵌套键的 checkpoint、完整模型、其他框架导出文件或只读推理包。当前本项目读取的是含 `model` 键的 PyTorch checkpoint，不能默认互通。

两个现有训练入口都没有赛事预训练参数初始化逻辑。把基线文件放进目录并不会自动加载，必须在任务构造/初始化代码中显式接入。

建议区分保存位置，例如：

```text
pretrained/                              # 建议新目录：基线原始文件，具体名字待定
checkpoints_detection/<实验编号>/
├── abnormal/best.pth                     # 本项目训练后的三分类权重
└── duplicate/best.pth                    # 本项目训练后的重复检测权重
```

对应配置的 `checkpoints_dir` 指向该实验目录。这些是建议位置，目前文件均未提供或生成。

接入时核对：构造参数、键名、参数形状、分类头维度和依赖版本。若有意只加载编码器，记录并解释缺失/额外键；不要用 `strict=False` 静默忽略错误后宣称整个模型加载成功。

训练初始化可以读取基线预训练参数；正式推理应读取训练后的本项目 checkpoint，避免构造函数自动下载或再次覆盖已训练参数。现有 `save_checkpoint(..., extra=...)` 可用于记录模型身份、类别顺序、预处理规格、划分标识和选模指标，无需先建立新的权重管理框架。

如果基线只提供不可训练的推理接口，不能假设能嵌入当前 PyTorch 反向传播流程；需要先确认它适合直接推理组合、独立训练，还是只能作为参考。

### 5.4 预处理必须随基线一起核对

现有 `load_preprocessed_volume()` 使用 NIfTI 数组读取、强度百分位处理、归一化和固定尺寸缩放；没有按基线要求自动统一体素间距或方向。默认目标尺寸 `[96, 128, 128]` 来自现有配置，不是新基线的要求。

每个基线都应核对：2D/3D、通道数、空间方向、体素间距、输入尺寸、强度范围、裁剪或取片方式，以及患者/病例/序列聚合。训练和推理共用相同的确定性处理，训练增强另行开启。

两个模型可复用原始影像读取，但只有输入要求完全相同，才适合复用同一个预处理张量。若采用新的增强，还应检查是否改变拼接类别的标签含义或抹除任务线索，不能默认所有医学影像增强都适合真实性判断。

## 6. 数据与评估如何支撑这三项能力

### 6.1 最少标签及目录

保持当前数据约定时，只需目标任务相关的：

- `1_abnormal.xlsx`：`AccessionNumber`、`SeriesUid`、`Label`，包含 true、fake、compositing。
- `2_duplicate.xlsx`：`src_img`、`desc_img`，代码将两列解释为病例编号，表内配对均按重复正样本处理。

当前读取目录为：

```text
annotation/<病例>/<序列>/<序列>.nii.gz                  # true，以及重复负样本
annotation/fake/<病例>/<序列>/<序列>.nii.gz             # fake
annotation/compositing/<病例>/<序列>/<序列>.nii.gz      # compositing
annotation/duplicate/<病例>/<序列>/<序列>.nii.gz        # 重复正样本两侧
```

目标训练不需要 `3_serieslabel.xlsx`、`4_masklabel.xlsx`、`5_characteristics.xlsx` 或分割 mask。真假/拼接三分类模型与重复模型仍各自需要充足的有效样本，不能只准备三个类别名称。

### 6.2 划分与负样本需要特别复核

现有异常训练按病例编号划分，分层标签取病例的第一条 `Label`；如果一个病例内标签不一致，需要先明确病例真值，不能默认第一条代表全病例。

现有重复训练对病例编号随机划分，只保留两端都在同一子集中的样本对，跨子集正样本会被丢弃；负样本先抽取再按子集过滤，最终数量比例不保证等于配置的 `negative_ratio`。比较不同基线前，应固定并保存实际保留的样本对。

还应根据真实数据核对：不同编号是否来自同一患者、同一原始影像或其拼接/重复版本。如果存在这种关系，应按来源分组隔离训练和验证；这是待核对的数据风险，本文不宣称当前数据已经发生泄漏。

负样本生成仅排除 `2_duplicate.xlsx` 明确列出的正样本对。若标注不是穷尽清单，或重复关系需要传递闭包，未列出的配对不一定都是可靠负样本，应根据官方标注约定处理。

### 6.3 最低评价内容

建议先建立固定的内部验证：

- 三分类：整体准确率、逐类 precision/recall/F1、混淆矩阵；明确按序列还是病例计算。
- 真假/拼接输出：分别检查 fake 和 compositing 的漏检、误报；二值阈值仅在独立验证集上确定并记录。
- 重复检测：固定正负配对的评价，以及批次内候选检索的覆盖情况。候选召回与候选上的判别分数需要分开解释。
- 工程运行：只存在两份目标权重时能启动、缺失模态/不等序列数/损坏影像的行为、保存后重新加载结果一致性。

这些是建议的开发评价，不是已核实的赛事评分标准。当前整体模拟脚本不覆盖全部三分类标签，也不能代替上述验证。

## 7. 改造顺序与输出约定

### 阶段 A：先得到现有两个任务的对照结果

在配置、数据和 Python 环境就绪后，下列是**当前已有命令**，从根目录运行：

```powershell
python -m train.train_abnormal --config configs/config.yaml
python -m train.train_duplicate --config configs/config.yaml
```

预期分别生成 `checkpoints/abnormal/best.pth`、`checkpoints/duplicate/best.pth` 及逐轮 checkpoint。训练日志位置由 `competition_log_dir` 决定，详见 README。执行命令会训练现有网络，不会自动采用未来赛事基线。

### 阶段 B：先解除完整推理依赖

新增两个任务的公共流程和命令行入口，三分类构造先使用现有模型，重复模型先保持现状。用只含两份目标权重的目录验证完整运行，不要求存在其他四份权重。

下面是**拟定命令，需实现文件并创建配置后才能执行**：

```powershell
python -m scripts.test_abnormal_duplicate --config configs/config_detection.yaml --input "./inference_input" --output "./outputs/detection_results"
```

拟定结果结构：

```text
outputs/detection_results/
├── <病例编号>/prediction.json
└── duplicate_pairs.jsonl
```

其中病例 JSON 保留真实计算出的 `AccessionNumber`、`IsNotHumanBodyProb`、`IsStitchedProb`，可另记计时；重复文件沿用 `StudyUID`、`StudyUID_dup`、`PairProb` 时，必须明确分值含义。现有 PairProb 为余弦映射值，不能把它宣称为校准后的概率。

不生成未执行任务的分割或肿瘤结果。赛事若要求完整字段，应依据协议处理缺省值或任务范围；不能填入虚构预测。官方是否接受精简结果，仍待核实。

### 阶段 C：逐个接入选定基线

先确认基线能在其规定环境下完成最小样例，再接入本项目。每次替换一个任务，核对预处理、输出、损失和 checkpoint 的保存/重载，然后在固定验证集比较。基线来源、代码版本、原始权重和训练参数均应记录。

若沿用训练 CLI，接入完成后仍可使用以下形式；**这里的新配置尚未创建，命令不是当前可直接运行的完整方案**：

```powershell
python -m train.train_abnormal --config configs/config_detection.yaml
python -m train.train_duplicate --config configs/config_detection.yaml
```

输出目录由新配置中的 `checkpoints_dir` 决定。不要在修改了输入规格或模型结构后继续把旧权重当作新实验结果。

### 阶段 D：统一服务与正式协议

本地两分支流程验证后，再修改服务的加载和后台调用。修改完成后拟定启动形式仍为：

```powershell
python -m service.server --config configs/config_detection.yaml
```

HTTP 任务结果继续写入 `<answer_root>/<evaluation_id>/`。请求字段、回调格式、任务粒度、必填输出、时限和资源要求需要对照实际赛事文档确认；当前代码中的接口实现不能单独证明它符合本届赛事要求。

## 8. 选定基线后必须补齐的信息

下面的信息决定具体代码，当前均不能凭模型名称或“有相关能力”推断：

1. **架构目标：**统一系统内两个模型，还是共享骨干的联合网络。
2. **基线来源：**确切名称、代码位置/版本、权重文件、训练及推理样例。
3. **输入约定：**NIfTI/DICOM/切片、2D/3D、通道数、方向、间距、尺寸和强度处理。
4. **分类能力：**是否直接支持这三个标签；如果不是，是否允许并支持训练三分类头。
5. **重复能力：**病例/序列/切片粒度，独立向量检索还是成对判别，输出是距离、相似度、logits 还是概率。
6. **可训练性与权重格式：**是否有可训练代码、使用什么框架、checkpoint 如何加载、哪些层允许或需要重新训练。
7. **数据关联：**是否有患者/原始影像来源标识，正负样本含义及重复标注是否完整。
8. **交付协议：**三项能力能否单独提交、结果字段与评分要求，以及实际运行环境限制。

在这些信息未齐全时，可以完成独立流程和对照评估设计；基线专属构造参数、预处理和最终提交协议应保持待定，不能写成已经实现或保证可用。

## 9. 实施完成的验收条件

以下是后续开发要验证的条件，本次撰写文档未执行这些检查：

- 只准备三分类和重复两份训练后权重，即可运行目标命令行流程；不再加载其他任务模型。
- 训练和推理使用同一结构、标签顺序、确定性预处理及同一任务评分规则。
- 使用实际小样本完成前向、训练反向（对可训练基线）、保存/重新加载与预测检查。
- 固定验证数据覆盖三分类各类及重复正负对；没有样本或指标未定义时明确标记，不虚报分数。
- 重复病例含不同序列数时处理正确；单病例批次、空数据和损坏文件的处理行为有明确记录。
- 新的 CLI 与服务对相同输入输出语义一致，错误不被写成正常概率。
- 新基线替换不意外改变原序列、分割、特征模型结构，也不覆盖需要保留的历史实验。
- 取得官方规范后，单独验证请求、输出、回调与评分协议。

本方案的实施重点是：把基线结构放进任务模型文件，把训练与推理接口对齐，再由一个公共流程组合三项能力。具体选型确定后，应按基线的真实接口更新第 4、5、8 节。
